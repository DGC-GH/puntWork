# puntWork Environment Configuration
# This file is ignored by git (.env is in .gitignore)
# ⚠️  WARNING: Never store actual passwords or credentials in this file!
# ⚠️  This is for DEVELOPMENT ONLY - not for production use!

# API Configuration
PUNTWORK_API_KEY=your_secure_api_key_here

# Development Settings
PUNTWORK_DEBUG=false
PUNTWORK_TEST_MODE=false

# WordPress Settings (for local development ONLY)
WP_DEBUG=true
WP_DEBUG_LOG=true
WP_ENV=development

# ⚠️  DO NOT PUT REAL CREDENTIALS HERE! ⚠️
# WordPress Database (for local development - use placeholders only)
DB_HOST=localhost
DB_NAME=puntwork_dev
DB_USER=db_user_placeholder
DB_PASSWORD=db_password_placeholder
DB_CHARSET=utf8mb4
DB_COLLATE=

# WordPress URLs (for local development)
WP_HOME=https://your-site.com/
WP_SITEURL=https://your-site.com/

# Authentication (NEVER STORE REAL PASSWORDS!)
WP_ADMIN_USER=admin@your-site.com
WP_ADMIN_EMAIL=admin@your-site.com
WP_ADMIN_PASSWORD=your_secure_password_here

# SMTP Settings (for local development)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# =================================================================
# 🚨 CRITICAL SECURITY WARNING 🚨
#
# This .env file is for LOCAL DEVELOPMENT ONLY!
#
# NEVER store real passwords, API keys, or credentials in this file.
# Even though it's in .gitignore, it can still be exposed through:
# - Accidental commits if .gitignore is not working
# - Local file system access
# - Backup systems
# - Development environment leaks
#
# For production, use:
# - WordPress admin interface for API keys
# - Secure password managers for credentials
# - Environment variables on your hosting platform
# - Proper secret management systems
#
# DELETE THIS FILE if you accidentally add real credentials!
# =================================================================